name: smflasher_nota
description: Aplikasi Nota SMFLASHER
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  pdf: ^3.10.7
  printing: ^5.11.1
  sqflite: ^2.3.2
  path_provider: ^2.1.4
  intl: ^0.20.3
  url_launcher: ^6.2.5
  share_plus: ^7.2.2
  screenshot: ^2.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/
