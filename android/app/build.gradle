plugins {
    id "com.android.application"
    id "kotlin-android"
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace "com.example.nama_app"  // GANTI SESUAI PUNYAMU
    compileSdkVersion 34

    defaultConfig {
        applicationId "com.example.nama_app" // GANTI SESUAI PUNYAMU
        minSdkVersion 21
        targetSdkVersion 34
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        release {
            signingConfig signingConfigs.debug
        }
    }
}

flutter {
    source '../..'
}
