plugins {
    id "com.android.application"
    id "org.jetbrains.kotlin.android"
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace "com.smflasher.nata"  // <-- GANTI INI SESUAI PACKAGE KAMU DI MANIFEST
    compileSdk 34

    defaultConfig {
        applicationId "com.smflasher.nata" // <-- SAMA KAYAK DI ATAS
        minSdk 21
        targetSdk 34
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        release {
            signingConfig signingConfigs.debug
        }
    }
}

flutter {
    source "../.."
}

dependencies {}
